# yaml-language-server: $schema=https://raw.githubusercontent.com/reubenmiller/go-c8y-cli/feat/extensions-manager/tools/schema/extension.json
version: "v1"
aliases:
  - name: listSimulators
    description: Example alias to list oee simulator device
    command: |
      c8y devices list --type "c8y_EventBasedSimulator"
    shell: true
